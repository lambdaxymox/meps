[package]
name = "meps"
version = "1.0.1"
authors = ["LambdaXymox <lambda.xymox@gmail.com>"]
edition = "2018"
description = "A shell utility that calculates the error bounds in representing floating point numbers."

[dependencies]
